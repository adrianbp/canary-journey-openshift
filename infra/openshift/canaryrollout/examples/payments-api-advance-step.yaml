apiVersion: canary.company.io/v1alpha1
kind: CanaryRollout
metadata:
  name: payments-api
  namespace: team-a
spec:
  targetRef:
    name: payments-api
    namespace: team-a
  planRef:
    configMapName: payments-api-rollout-plan
  action: ADVANCE_STEP
  stepName: step-25
  mode:
    type: MANUAL
    waitSeconds: 120
  approval:
    required: true
    state: APPROVED
    approvedBy: release-manager@company.com
  request:
    requestedBy: release-manager@company.com
    changeTicket: CHG-1002
    reason: "Increase canary traffic to 25%"
    idempotencyKey: "payments-api-step25-20260311"
