server:
  port: 8082

spring:
  application:
    name: manual-gate-webhook

approval-api:
  base-url: http://localhost:8081

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
