{{- if and .Values.canary.enabled .Values.primary.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.primary.serviceName }}
  namespace: {{ .Values.app.namespace }}
  labels:
{{ include "canary-addon.labels" . | indent 4 }}
spec:
  selector:
    app: {{ .Values.primary.deploymentName }}
  ports:
    - name: http
      port: 80
      targetPort: 8080
{{- end }}
